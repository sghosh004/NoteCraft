<!-- <!DOCTYPE html>
<html lang="en">
  <head>
    <title>Admin Notes</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
  </head>
  <body>
    <div class="container">
      <h1>Unverified Notes</h1>
      <table class="table">
        <thead>
          <tr>
            <th>Subject</th>
            <th>Year</th>
            <th>Type</th>
            <th>Uploaded By</th>
            <th>Actions</th>
          </tr>
        </thead>
        <tbody>
          <% files.forEach(file => { %>
          <tr>
            <td><%= file.subject %></td>
            <td><%= file.year %></td>
            <td><%= file.type %></td>
            <td><%= file.uploadedBy%></td>
            <td>
              <form
                action="/admin/verify-note"
                method="POST"
                style="display: inline"
              >
                <input type="hidden" name="noteId" value="<%= file._id %>" />
                <button type="submit" class="btn btn-success btn-sm">
                  Verify
                </button>
              </form>
              <form
                action="/admin/delete-note"
                method="POST"
                style="display: inline"
              >
                <input type="hidden" name="noteId" value="<%= file._id %>" />
                <button type="submit" class="btn btn-danger btn-sm">
                  Delete
                </button>
              </form>
            </td>
          </tr>
          <% }) %>
        </tbody>
      </table>
    </div>
  </body>
</html> -->

<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Admin Notes</title>
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
    />
    <style>
      body {
        color: #ffffff;
        background: linear-gradient(-45deg, #000000, #1a1a1a, #121212, #0a0a0a);
        background-size: 400% 400%;
        animation: gradientBG 15s ease infinite;
      }
      @keyframes gradientBG {
        0% {
          background-position: 0% 50%;
        }
        50% {
          background-position: 100% 50%;
        }
        100% {
          background-position: 0% 50%;
        }
      }
      .title {
        font-size: 2.5rem;
        font-weight: bold;
        background: linear-gradient(45deg, #ffffff, #4caf50);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 10px rgba(76, 175, 80, 0.5);
      }
      .btn-success {
        background: linear-gradient(45deg, #4caf50, #45a049);
        border: none;
        transition: all 0.3s ease;
      }
      .btn-success:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(76, 175, 80, 0.4);
        background: linear-gradient(45deg, #45a049, #4caf50);
      }
      .btn-danger {
        background: linear-gradient(45deg, #ff5252, #ff1744);
        border: none;
        transition: all 0.3s ease;
      }
      .btn-danger:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(255, 23, 68, 0.4);
        background: linear-gradient(45deg, #ff1744, #ff5252);
      }
      .card {
        background-color: rgba(0, 0, 0, 0.7);
        border: none;
        border-radius: 10px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.4);
      }
    </style>
  </head>
  <body>
    <div class="container my-5">
      <div class="text-center mb-4">
        <h1 class="title">Unverified Notes</h1>
        <p class="text-light">
          Admin panel to verify or delete uploaded academic resources.
        </p>
      </div>
      <div class="card p-4">
        <div class="table-responsive">
          <table class="table table-hover table-dark table-bordered">
            <thead>
              <tr class="text-center">
                <th>Subject</th>
                <th>Year</th>
                <th>Type</th>
                <th>Uploaded By</th>
                <th>View File</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              <% files.forEach(file => { %>
              <tr class="text-center">
                <td><%= file.subject %></td>
                <td><%= file.year %></td>
                <td><%= file.type %></td>
                <td><%= file.uploadedBy %></td>
                <td>
                  <a
                    href="/uploads/<%= file.storedName %>"
                    target="_blank"
                    class="btn btn-info btn-sm"
                  >
                    View
                  </a>
                </td>
                <td>
                  <form
                    action="/admin/verify-note"
                    method="POST"
                    class="d-inline"
                  >
                    <input
                      type="hidden"
                      name="noteId"
                      value="<%= file._id %>"
                    />
                    <button type="submit" class="btn btn-success btn-sm">
                      Verify
                    </button>
                  </form>
                  <form
                    action="/admin/delete-note"
                    method="POST"
                    class="d-inline"
                  >
                    <input
                      type="hidden"
                      name="noteId"
                      value="<%= file._id %>"
                    />
                    <button type="submit" class="btn btn-danger btn-sm">
                      Delete
                    </button>
                  </form>
                </td>
              </tr>
              <% }) %>
            </tbody>
          </table>
        </div>
      </div>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>
