<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
      rel="stylesheet"
      integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH"
      crossorigin="anonymous"
    />
    <style>
      body {
        color: #ffffff;
        background: linear-gradient(-45deg, #000000, #1a1a1a, #121212, #0a0a0a);
        background-size: 400% 400%;
        animation: gradientBG 15s ease infinite;
      }
      @keyframes gradientBG {
        0% { background-position: 0% 50% }
        50% { background-position: 100% 50% }
        100% { background-position: 0% 50% }
      }
      .overlay {
        background: radial-gradient(circle at center, transparent, rgba(0, 0, 0, 0.3));
      }
      .logo {
        font-size: 24px;
        font-weight: bold;
        background: linear-gradient(45deg, #ffffff, #4caf50);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 10px rgba(76, 175, 80, 0.5);
      }
      .title-container {
        display: inline-block;
        position: relative;
        padding-right: 3px; /* Add padding for cursor */
      }
      .title {
        font-size: 55px;
        background: linear-gradient(45deg, #ffffff, #4caf50);
        -webkit-background-clip: text;
        -webkit-text-fill-color: transparent;
        text-shadow: 0 0 10px rgba(76, 175, 80, 0.5);
        white-space: nowrap;
        overflow: hidden;
        position: relative;
        margin: 0 auto 20px;
        border-right: 3px solid #4caf50; /* Add visible cursor */
        width: 0;
        animation: 
          typing 3.5s steps(40, end) forwards,
          blink-caret 0.75s step-end infinite;
      }
      @keyframes typing {
        from { width: 0 }
        to { width: 100% }
      }
      @keyframes blink-caret {
        from, to { border-color: transparent }
        50% { border-color: #4caf50 }
      }
      .subtitle {
        opacity: 0;
        animation: fadeIn 1s ease-in forwards;
        animation-delay: 3.5s;
      }
      .btn-container {
        opacity: 0;
        animation: fadeIn 1s ease-in forwards;
        animation-delay: 4s;
      }
      @keyframes fadeIn {
        from { opacity: 0; }
        to { opacity: 1; }
      }
      .btn-success {
        background: linear-gradient(45deg, #4caf50, #45a049);
        border: none;
        transition: all 0.3s ease;
      }
      .btn-success:hover {
        transform: translateY(-2px);
        box-shadow: 0 4px 15px rgba(76, 175, 80, 0.4);
        background: linear-gradient(45deg, #45a049, #4caf50);
      }
    </style>
  </head>
  <body>
    <div class="overlay d-flex flex-column min-vh-100">
      <%- include('navbar') %>
      <main
        class="flex-grow-1 d-flex flex-column align-items-center justify-content-center text-center p-3"
      >
        <div class="title-container">
          <h1 class="title">Empower Your Academic Journey</h1>
        </div>
        <p class="subtitle text-light mb-4">
          NoteCraft is the ultimate resource aggregator for T.I.E.T. students.
          Upload, search, download, and collaborate on academic resources.
        </p>
        <div class="d-flex gap-2 mx-auto center btn-container">
          <a href="/search" class="btn btn-success btn-lg">Search</a>
          <a href="/upload" class="btn btn-success btn-lg">Upload</a>
        </div>
      </main>
    </div>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
  </body>
</html>